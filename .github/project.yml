# yaml-language-server: $schema=https://raw.githubusercontent.com/cuioss/cuioss-organization/main/.github/actions/read-project-config/schema.json
name: cui-test-value-objects

release:
  current-version: 2.1.2
  next-version: 2.1-SNAPSHOT
  create-github-release: true

sonar:
  project-key: cuioss_cui-test-value-objects

pages:
  reference: cui-test-value-objects

github-automation:
  auto-merge-build-versions: true
  auto-merge-build-timeout: 300

consumers:
  - cuioss-parent-pom:version.cui.test.value.objects
  - nifi-extensions:version.cui.test.value.objects

dependency-propagation:
  group-id: de.cuioss.test
  artifact-id: cui-test-value-objects
  scope: dependency
